<?xml version="1.0" encoding="UTF-8"?>
<Wix xmlns="http://schemas.microsoft.com/wix/2006/wi">
	<Fragment>
    <!-- =======================================================================================  -->
    <!-- FILE CONFIG COMPONENT GROURS: Well known static files -->
    <!-- This are config files with KeyPath="no" which are not overwritten by update if available -->
    <!-- file is updated by user                                                                  -->
    <!-- !!! VERY IMPORTANT !!!: EXTREM WICHTIGE ENTSCHEIDUNG FUER CONFIG-FILES !!!
             - Nutze KeyPath="no".
             - Setze einmalig eine GUID für ein ConfigFile fest die sich NIE ändern darf.
             - Nutze Permament="yes"
             - Nutze NeverOverwrite="yes"
    -->
    <!-- =======================================================================================  -->

    <ComponentGroup Id="cgrp_File_TestService02Config" Directory="dir_PROJECT_CONFIG_DIR_FOR_MODULE">
      <Component Id="CMP_TestService02Config" Guid="6E09C027-6FDC-4C22-9C44-8DCEC56BC6F4" Permanent="yes" NeverOverwrite="yes">
        <File Id="FILE_TestService02Config"  Source="$(var.DropFolderModuleConfiguration)\TestService02.Config.json" KeyPath="no" />
      </Component>
    </ComponentGroup>
    
	</Fragment>
</Wix>
